<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 사이트 : 댓글 관련 -->
<mapper namespace="boardCmmentDAO">

    <!-- 댓글 목록 -->
    <select id="siteBoardCommentList" resultType="boardCommentDTO">
    </select>


	<!-- 해당 게시판 > 해당 게시글 > 댓글 리스트 출력 -->
	<select id="SiteBoardCommList" parameterType="map" resultType="boardCommentDTO">
		select * from board_comment_${bbs_id} where bdata_no = #{bdata_no}
	</select>

	<!-- 해당 게시판 > 해당 게시글 > 해당 댓글 삭제 -->
	<delete id="SiteBoardCommDelete" parameterType="map">
		delete from board_comment_${bbs_id} where bdata_no = #{bdata_no} and bcomm_no = #{bcomm_no}
	</delete>
	
	<insert id="SiteBoardCommentInsert" parameterType="map">
		
	   <selectKey resultType="int" keyProperty="dto.bcomm_no" order="BEFORE">
           select max(bcomm_no) from board_comment_${bbs_id} where bdata_no = #{dto.bdata_no}
       </selectKey>
       
       insert into board_comment_${bbs_id} values(#{dto.bcomm_no}+1,#{dto.bdata_no},#{dto.bcomm_id},#{dto.bcomm_pw},#{dto.bcomm_name},#{dto.bcomm_cont},sysdate)
	
	</insert>



</mapper>